const express = require('express')
const db = require('../dao/daoClientesMemoria')




const router = express.Router()

const path = ''
router.getAll('/clientes', (req,res)=>{
    // res.sendFile(path)
    try {
        let clientes
        clientes = await de
        
    } catch (error) {
        
    }
})


module.exports = router