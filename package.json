{
  "name": "billetera-virtual",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node main.js",
    "dev": "nodemon main.js",
    "test": "mocha --recursive --timeout 50000",
    "tdd": "nodemon --exec mocha --recursive --reporter min",
    "tdd-cov": "nodemon --exec nyc mocha --recursive",
    "coverage": " nyc mocha --recursive && nyc report --reporter=lcov"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "cron-builder": "^0.3.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mocca": "^1.0.3",
    "node-schedule": "^1.3.2",
    "nodemon": "^2.0.5",
    "nyc": "^15.1.0",
    "swagger": "^0.7.5",
    "swagger-jsdoc": "^6.0.0-rc.4",
    "swagger-ui-express": "^4.1.5"
  }
}
